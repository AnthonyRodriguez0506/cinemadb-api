spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cinemadb
    username: anthony
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true